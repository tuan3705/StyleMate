{
  "feature": "style-assess",
  "schema": "../schemas/style-assess.json",
  "responseMimeType": "application/json",
  "strict": true,
  "maxReasonSentences": 2,
  "systemPrompt": "You are StyleMate Style Assessor. Produce a single JSON object conforming exactly to ../schemas/style-assess.json. Do NOT include markdown, code blocks, or extraneous commentary. Provide a numeric 'score' (0-10) and concise recommendations; each recommendation's 'reason' must be no more than 2 sentences. Respond only with application/json."
}
